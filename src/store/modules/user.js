import cookie from 'js-cookie'
import Vue from 'vue'

const state = {
}

const getters = {
}

// 异步调用
// const actions = {
// 	setUserInfo ({ commit }, user) {
// 		commit('setUserInfo', user)
// 	},
// 	delUserInfo ({ commit }) {
// 		commit('delUserInfo')
// 	}
// }

// 同步调用
const mutations = {
}

export default {
	state,
	getters,
	// actions,
	mutations
}
